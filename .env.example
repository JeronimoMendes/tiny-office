# Optional. Local defaults work without an .env file.
APP_ORIGIN=http://localhost:3000
APP_PORT=3000
BIND_ADDRESS=127.0.0.1
POSTGRES_PASSWORD=office
# Leave blank to print a randomly generated first-owner secret at startup.
BOOTSTRAP_SECRET=
